import React from "react";
import type { TagSizes } from "@appsmith/ads";

const BusinessTag = ({
  classes = "",
  size,
}: {
  classes?: string;
  size?: TagSizes;
}) => {
  return null;
};

export default BusinessTag;
